# Core ML/DL packages
# NOTE: Install PyTorch with CUDA support first using the appropriate command:
# For CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Then install the rest of the requirements with: pip install -r requirements.txt

transformers>=4.30.0
datasets>=2.14.0

# Tensor operations and utilities
einops>=0.6.1
triton>=2.2.0

# Data handling
# NOTE: Install pyarrow separately with: pip install pyarrow --only-binary=:all:
# (pyarrow requires CMake 3.25+, which may not be available, so use pre-built wheels)

# Training utilities
tqdm>=4.65.0
numpy>=1.24.0
safetensors>=0.3.0
packaging
accelerate>=0.20.0

# AWS integration (for SageMaker and S3)
boto3>=1.26.0
sagemaker>=2.150.0

# HuggingFace integration
huggingface_hub>=0.15.0

# Monitoring and logging
tensorboard>=2.13.0
wandb>=0.15.0
